# MarkTheDate 前端

## 本地开发

```bash
cd frontend
npm install
npm run dev
# 默认代理 /api 到 http://localhost:8000
```

访问 http://localhost:3000

## Docker 构建

```bash
docker build -t markthedate-frontend .
```